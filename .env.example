# Optional local bootstrap. The recommended flow is Admin -> Integrations -> Spotify -> Setup.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
# Optional legacy/manual override. Leave empty when using the setup UI + OAuth connect flow.
SPOTIFY_REFRESH_TOKEN=
